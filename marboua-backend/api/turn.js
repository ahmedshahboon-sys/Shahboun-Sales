import{getUser,json,cors}from'../lib/supabase.js';
export default async function handler(req,res){if(cors(req,res))return;if(req.method!=='GET')return json(res,405,{error:'method_not_allowed'});const user=await getUser(req);if(!user)return json(res,401,{error:'unauthorized'});const urls=(process.env.TURN_URLS||'').split(',').map(x=>x.trim()).filter(Boolean),username=process.env.TURN_USERNAME||'',credential=process.env.TURN_CREDENTIAL||'';if(!urls.length||!username||!credential)return json(res,503,{error:'turn_not_configured'});return json(res,200,{iceServers:[{urls,username,credential}],ttl:3600})}
