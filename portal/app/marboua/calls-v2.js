import{createClient}from'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
const U='https://uojytcmeqegnlgvlmexx.supabase.co',K='sb_publishable_IB9p4LAK2s7VLW1u55SJQA_pANuxzHl',s=createClient(U,K);
const el=id=>document.getElementById(id);
let me=null,roomId=null,peerId=null,peerName='مستخدم',call=null,pc=null,stream=null,signalChannel=null,queuedIce=[],pendingOffer=null,ringTimer=null,durationTimer=null,startedAt=null,patched=false,origOpenRoom=null;
function ensureUI(){el('callControls')?.remove();el('v2Controls')?.remove();if(!el('remoteAudio')){const a=document.createElement('audio');a.id='remoteAudio';a.autoplay=true;a.playsInline=true;a.volume=1;a.style.display='none';document.body.appendChild(a)}if(!el('playRemoteAudio')){const b=document.createElement('button');b.id='playRemoteAudio';b.className='btn primary hide';b.textContent='🔊 تشغيل الصوت';b.onclick=()=>playRemote();el('callState')?.insertAdjacentElement('afterend',b)}const box=el('callOverlay')?.querySelector('.callBox');if(box&&!el('v2Controls')){const row=document.createElement('div');row.id='v2Controls';row.style='display:flex;gap:10px;justify-content:center;flex-wrap:wrap;margin-top:16px';row.innerHTML='<button class="btn" id="v2Mute">🎙️ كتم</button><button class="btn" id="v2Speaker">🔊 سبيكر</button><button class="btn" id="v2Camera">📷 كاميرا</button>';box.appendChild(row);el('v2Mute').onclick=toggleMute;el('v2Speaker').onclick=toggleSpeaker;el('v2Camera').onclick=toggleCamera}if(!el('marbCallV2Style')){const st=document.createElement('style');st.id='marbCallV2Style';st.textContent='.audio-call .videos{display:none!important}.audio-call .callBox{max-width:420px}.audio-call #v2Camera{display:none!important}.audio-call #callTitle{margin-bottom:8px}#callControls{display:none!important}#v2Controls+.danger{display:none!important}';document.head.appendChild(st)}}
function setMode(type,incoming=false){ensureUI();const o=el('callOverlay');if(!o)return;o.classList.toggle('audio-call',type==='audio');o.classList.remove('hide');el('callTitle').textContent=(type==='video'?'مكالمة فيديو':'مكالمة صوتية')+' · '+peerName;el('callState').textContent=incoming?'مكالمة واردة':'جاري الاتصال...';el('answerBtn')?.classList.toggle('hide',!incoming);el('playRemoteAudio')?.classList.add('hide')}
async function media(type){if(!isSecureContext||!navigator.mediaDevices?.getUserMedia)throw new Error('افتح مربوعة من Chrome عبر HTTPS');return navigator.mediaDevices.getUserMedia({audio:{echoCancellation:true,noiseSuppression:true,autoGainControl:true},video:type==='video'?{facingMode:'user'}:false})}
function makePc(type){queuedIce=[];pc?.close();pc=new RTCPeerConnection({iceServers:[{urls:'stun:stun.l.google.com:19302'},{urls:'stun:stun1.l.google.com:19302'}]});stream.getTracks().forEach(t=>pc.addTrack(t,stream));pc.ontrack=e=>{const rs=e.streams[0]||new MediaStream([e.track]);if(type==='video')el('remoteVideo').srcObject=rs;const a=el('remoteAudio');a.srcObject=rs;a.muted=false;a.volume=1;playRemote()};pc.onicecandidate=e=>{if(e.candidate&&call&&peerId)send('ice',e.candidate)};pc.oniceconnectionstatechange=updateState;pc.onconnectionstatechange=updateState;return pc}
async function playRemote(){const a=el('remoteAudio');if(!a?.srcObject)return;try{a.muted=false;a.volume=1;await a.play();el('playRemoteAudio')?.classList.add('hide')}catch{el('playRemoteAudio')?.classList.remove('hide');el('callState').textContent='متصل · اضغط تشغيل الصوت'}}
function updateState(){if(!pc)return;const c=pc.connectionState,i=pc.iceConnectionState;if(c==='connected'||i==='connected'||i==='completed'){stopRing();if(!startedAt){startedAt=Date.now();durationTimer=setInterval(()=>{const sec=Math.floor((Date.now()-startedAt)/1000),m=Math.floor(sec/60),ss=String(sec%60).padStart(2,'0');el('callState').textContent='متصل · '+m+':'+ss},1000)}playRemote()}else if(c==='failed'||i==='failed')el('callState').textContent='تعذر تمرير الصوت بين الشبكتين'}
async function drainIce(){if(!pc?.remoteDescription)return;for(const c of queuedIce.splice(0)){try{await pc.addIceCandidate(new RTCIceCandidate(c))}catch{}}}
async function send(type,payload,to=peerId){if(call?.id&&to)await s.from('marboua_call_signals').insert({call_id:call.id,sender_id:me.id,receiver_id:to,signal_type:type,payload})}
async function resolvePeer(room){const{data}=await s.from('marboua_room_members').select('user_id').eq('room_id',room);peerId=(data||[]).find(x=>x.user_id!==me.id)?.user_id||null;if(peerId){const{data:p}=await s.from('marboua_profiles').select('display_name').eq('user_id',peerId).maybeSingle();peerName=p?.display_name||'مستخدم'}return peerId}
async function start(type){if(!roomId||!peerId){if(roomId)await resolvePeer(roomId);if(!peerId)return alert('اختار محادثة فردية أول.')}try{stream=await media(type)}catch(e){return alert('ما قدرناش نفتح الميكروفون/الكاميرا: '+(e.message||e))}setMode(type);el('localVideo').srcObject=stream;makePc(type);const{data:c,error}=await s.from('marboua_calls').insert({room_id:roomId,caller_id:me.id,callee_id:peerId,call_type:type,status:'ringing'}).select().single();if(error){cleanup();return alert(error.message)}call=c;const offer=await pc.createOffer();await pc.setLocalDescription(offer);await send('offer',offer);startRing('outgoing');setTimeout(()=>{if(call&&pc?.connectionState!=='connected')el('callState').textContent='يرن...'},300)}
async function incoming(c,offer){if(call&&call.id!==c.id)return;call=c;peerId=c.caller_id;pendingOffer=offer;const{data:p}=await s.from('marboua_profiles').select('display_name').eq('user_id',peerId).maybeSingle();peerName=p?.display_name||'مستخدم';roomId=c.room_id;setMode(c.call_type,true);el('answerBtn').onclick=answer;startRing('incoming');navigator.vibrate?.([450,250,450])}
async function answer(){if(!call||!pendingOffer)return;try{stream=await media(call.call_type)}catch{return alert('اسمح بالميكروفون ثم جرّب الرد.')}el('localVideo').srcObject=stream;makePc(call.call_type);await pc.setRemoteDescription(new RTCSessionDescription(pendingOffer));await drainIce();const ans=await pc.createAnswer();await pc.setLocalDescription(ans);await send('answer',ans,call.caller_id);pendingOffer=null;el('answerBtn')?.classList.add('hide');stopRing();el('callState').textContent='جاري توصيل الصوت...'}
async function onSignal(sig){if(sig.receiver_id!==me.id)return;if(sig.signal_type==='offer'){const{data:c}=await s.from('marboua_calls').select('*').eq('id',sig.call_id).maybeSingle();if(c)await incoming(c,sig.payload);return}if(!call||sig.call_id!==call.id)return;if(sig.signal_type==='answer'&&pc){await pc.setRemoteDescription(new RTCSessionDescription(sig.payload));await drainIce()}else if(sig.signal_type==='ice'){if(pc?.remoteDescription){try{await pc.addIceCandidate(new RTCIceCandidate(sig.payload))}catch{}}else queuedIce.push(sig.payload)}else if(sig.signal_type==='hangup'||sig.signal_type==='reject')cleanup()}
function startRing(kind){stopRing();let ctx;try{ctx=new(window.AudioContext||window.webkitAudioContext)()}catch{return}const play=()=>{try{ctx.resume?.();const o=ctx.createOscillator(),g=ctx.createGain();o.frequency.value=kind==='incoming'?740:440;g.gain.value=kind==='incoming'?.07:.035;o.connect(g);g.connect(ctx.destination);o.start();o.stop(ctx.currentTime+.28)}catch{}};play();ringTimer=setInterval(play,kind==='incoming'?1200:1800)}function stopRing(){clearInterval(ringTimer);ringTimer=null}
function toggleMute(){const t=stream?.getAudioTracks?.()[0];if(!t)return;t.enabled=!t.enabled;el('v2Mute').textContent=t.enabled?'🎙️ كتم':'🔇 فتح المايك'}async function toggleSpeaker(){const a=el('remoteAudio');if(a){a.volume=1;await playRemote()}}function toggleCamera(){const t=stream?.getVideoTracks?.()[0];if(t){t.enabled=!t.enabled;el('v2Camera').textContent=t.enabled?'📷 كاميرا':'🚫 فتح الكاميرا'}}
async function finish(status='ended'){if(call){await send(status==='rejected'?'reject':'hangup',{},peerId).catch(()=>{});await s.from('marboua_calls').update({status,ended_at:new Date().toISOString()}).eq('id',call.id).catch(()=>{})}cleanup()}
function cleanup(){stopRing();clearInterval(durationTimer);durationTimer=null;startedAt=null;pc?.close();pc=null;stream?.getTracks().forEach(t=>t.stop());stream=null;queuedIce=[];pendingOffer=null;call=null;const a=el('remoteAudio');if(a){a.pause();a.srcObject=null}el('callOverlay')?.classList.add('hide');el('answerBtn')?.classList.add('hide')}
async function pollIncoming(){if(call)return;const{data:c}=await s.from('marboua_calls').select('*').eq('callee_id',me.id).eq('status','ringing').order('started_at',{ascending:false}).limit(1);const x=c?.[0];if(!x||Date.now()-new Date(x.started_at).getTime()>45000)return;const{data:o}=await s.from('marboua_call_signals').select('*').eq('call_id',x.id).eq('receiver_id',me.id).eq('signal_type','offer').order('created_at',{ascending:false}).limit(1);if(o?.[0])incoming(x,o[0].payload)}
function patch(){if(patched||!window.startCall||!window.openRoom)return;patched=true;ensureUI();origOpenRoom=window.openRoom;window.openRoom=async(id,name,type)=>{roomId=id;if(type==='direct'){peerName=name||'مستخدم';await resolvePeer(id)}return origOpenRoom(id,name,type)};window.startCall=start;window.answerCall=answer;window.endCall=()=>finish('ended');window.rejectMarbouaCall=()=>finish('rejected');signalChannel=s.channel('marboua-v2-'+me.id).on('postgres_changes',{event:'INSERT',schema:'public',table:'marboua_call_signals',filter:'receiver_id=eq.'+me.id},p=>onSignal(p.new)).subscribe();setInterval(pollIncoming,1800)}
async function init(){const{data:{user}}=await s.auth.getUser();me=user;if(!me)return;let n=0;const t=setInterval(()=>{if(window.startCall&&window.openRoom){clearInterval(t);patch()}else if(++n>40)clearInterval(t)},100)}init();